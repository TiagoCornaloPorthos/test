{
  "name": "inbound-wires-push",
  "version": "0.0.1",
  "description": "Push content to ARC.",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "date-fns": "^2.14.0",
    "form-data": "^4.0.0"
  },
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^26.0.1"
  },
  "jest": {
    "moduleNameMapper": {
      "/opt/nodejs/(.*)": "<rootDir>/../layers/LibLayer/$1"
    }
  }
}
